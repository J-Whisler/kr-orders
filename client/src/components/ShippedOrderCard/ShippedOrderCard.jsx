import React from "react";
import "./ShippedOrderCard.scss";

const ShippedOrderCard = ({ order }) => {
  return <div className="shippedOrderCard">Shipped Order Card</div>;
};

export default ShippedOrderCard;
